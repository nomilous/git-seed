// Generated by CoffeeScript 1.4.0
var sync;

module.exports = sync = {
  series: function(targetsArray, functionName, args, endCallback) {
    var target, targets, _i, _len;
    targets = [];
    for (_i = 0, _len = targetsArray.length; _i < _len; _i++) {
      target = targetsArray[_i];
      targets.push(target);
    }
    args.push(function(err, result) {
      return sync.recurse(targets, functionName, args, endCallback);
    });
    return sync.recurse(targets, functionName, args, endCallback);
  },
  recurse: function(targets, fname, args, ecb) {
    var target;
    target = targets.shift();
    if (!target) {
      return;
    }
    return target[fname].apply(target, args);
  }
};
