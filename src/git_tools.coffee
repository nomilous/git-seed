module.exports = gitTools = 

    showOrigin: (workDir) -> 

        'get origin for' + workDir

    showBranch: (workDir) -> 

        'get branch for' + workDir

    showRef: (workDir) -> 

        'get ref for' + workDir

